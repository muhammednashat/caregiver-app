<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".util.MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyProject"
        tools:targetApi="31">
        <activity
            android:name=".users.patient.main.tools.breath.BreathActivity"
            android:exported="false" />
        <activity
            android:name=".maybeneedlater.TasksActivity"
            android:exported="false" />
        <activity
            android:name=".users.patient.dailyprogram.DailyProgramActivity"
            android:exported="false" />
        <activity
            android:name=".users.patient.supporters.SupportersActivity"
            android:exported="false" />
        <activity
            android:name=".users.patient.editprofile.EditProfileActivity"
            android:exported="false" />
        <activity
            android:name=".util.TermsPrivacyActivity"
            android:exported="false" />
        <activity
            android:name=".users.admin.AdminScreenActivity"
            android:exported="false"
            android:theme="@style/Theme.MyProject" />
        <activity
            android:name=".users.patient.main.UserScreensActivity"
            android:exported="false" />
        <activity
            android:name=".users.caregiver.CaregiverScreenActivity"
            android:exported="false" />
        <activity
            android:name=".auth.AuthActivity"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".ui.user.intro.IntroActivity" -->
        <!-- android:exported="true"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->
        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->
        <activity
            android:name=".users.patient.intro.IntroActivity"
            android:exported="false" />
        <activity
            android:name=".util.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".firebase.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>